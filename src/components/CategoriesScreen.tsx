import CategorySwipeScreen from "./category-swipe/CategorySwipeScreen";

/** Cardápio — deck Deslizar como tela inicial. */
export default function CategoriesScreen() {
  return <CategorySwipeScreen />;
}
